

{% block title %}Hello SearchController!{% endblock %}

{% block body %}
<style>
   
</style>

    <nav id="navbar" class="navbar navbar-expand-lg navbar-dark bg-dark" style="height:400px;">
        <div class="container-fluid">
          <a class="navbar-brand" href="#"> <img src="{{ asset('images/logo.png') }}" alt="Logo Wikibrain" width="180" height="48"/></a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" 
          data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" 
          aria-label="Toggle navigation" >
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                
                <a class="nav-link active" aria-current="page" href="{{ path("index") }}">
                  Home
                </a>
              </li>
              {# <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{ path("inscription") }}">S'inscrire</a>
              </li> #}
              <li class="nav-item">
              {%if app.user %}
                <a class="nav-link" href="{{ path("logout") }}">Logout</a>
                {% else %}
                <a class="nav-link" href="{{ path("login") }}">Login</a>
                {% endif %}
              </li>
            
              {% if app.user %}
              <li class="nav-item">
                <a class="nav-link" href="{{ path("fiche_create") }}" tabindex="-1" aria-disabled="false">Créer fiche</a>
              </li>
              {% endif %}
            </ul>
            <form class="d-flex">

              <ul class="navbar-nav me-auto mb-2 mb-lg-0 search_menu">
                <li class="nav-item">
                  
                  <a class="nav-link" aria-current="page" href="index.html">
                     Trier 
                  </a>
                </li>

                <li class="nav-item">
                  
                  <a class="nav-link" aria-current="page" href="index.html">
                     Categories 
                  </a>
                </li>

                </ul>

              <input  class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
              <a href="" ><button id="search_btn_id" class="btn search_btn" type="submit" >Search</button></a>
            </form>
          </div>
        </div>
      </nav>
{% endblock %}
